# Mail-to-NotebookLM 环境变量
# 复制为 .env 并填入实际值（仅本地开发时使用，GitHub Actions 请在 Secrets 中配置）

# --- 邮箱凭据（必填）---
EMAIL_USERNAME=your-email@gmail.com
EMAIL_PASSWORD=your-app-password

# --- 邮箱服务器（可选，覆盖 config.yaml 中的设置）---
# EMAIL_IMAP_HOST=imap.gmail.com
# EMAIL_IMAP_PORT=993
# EMAIL_SMTP_HOST=smtp.gmail.com
# EMAIL_SMTP_PORT=587

# --- 授权发件人（可选，逗号分隔，覆盖 config.yaml）---
# AUTH_ALLOWED_SENDERS=user1@example.com,user2@example.com

# --- 主题密钥（如启用）---
# AUTH_SUBJECT_KEY=my-secret-key

# --- NotebookLM 集成（选择其一）---

# 方案 A: notebooklm-py（个人用户推荐）
# 先在本地运行 notebooklm-py 完成浏览器认证，然后导出 auth JSON
# NOTEBOOKLM_AUTH_JSON={"cookies": [...]}

# 方案 B: Enterprise API（需要 Google Workspace Enterprise 许可）
# NOTEBOOKLM_INTEGRATION=enterprise_api
# GCP_PROJECT_NUMBER=123456789
# GCP_CREDENTIALS_JSON={"type": "service_account", ...}
